{
  "name": "porter-workspace",
  "private": true,
  "version": "0.0.0",
  "description": "Turborepo workspace for the Porter website and shared packages.",
  "workspaces": [
    "apps/web",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "preview": "turbo run preview"
  },
  "devDependencies": {
    "turbo": "^2.5.6"
  },
  "packageManager": "bun@1.3.11"
}
